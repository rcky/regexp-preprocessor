"use strict";

var Class = require('ee-class')
    , Types = require('ee-types')
    , log = require('ee-log');

var GroupNode = {
    init: function initialize() {
        //initialize.parent();
    }
};

module.exports = new Class(GroupNode);